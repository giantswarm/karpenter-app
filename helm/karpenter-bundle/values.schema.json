{
    "$schema": "http://json-schema.org/schema#",
    "type": "object",
    "additionalProperties": true,
    "properties": {
        "clusterID": {
            "type": "string"
        },
        "helmRelease": {
            "type": "object",
            "properties": {
                "dependsOn": {
                    "type": "array",
                    "description": "HelmReleases the karpenter HelmRelease depends on, named without the cluster ID prefix. Each entry is evaluated as a Go template, so a dependency can be made conditional; entries resulting in an empty string are skipped.",
                    "items": {
                        "type": "string"
                    }
                }
            }
        },
        "nodeIamRole": {
            "type": "object",
            "properties": {
                "create": {
                    "type": "boolean",
                    "description": "Create the IAM role and instance profile for karpenter-launched nodes. Required on EKS clusters, where no worker instance profile exists."
                }
            }
        },
        "ociRepositoryUrl": {
            "type": "string"
        },
        "region": {
            "type": "string"
        },
        "workersIamRole": {
            "type": "string"
        }
    }
}
